# Telegram Bot для управления воркерами и аккаунтами Telethon

Этот проект представляет собой Telegram-бота для управления воркерами и аккаунтами Telethon. Воркеры могут находиться на разных серверах и запускать аккаунты Telethon в разных потоках.

## Особенности

- Создание и управление воркерами на удаленных серверах
- Автоматическое обнаружение новых воркеров через Redis
- Добавление аккаунтов Telethon к воркерам
- Запуск и остановка аккаунтов
- Мониторинг статуса воркеров и аккаунтов в реальном времени через Redis
- Асинхронное взаимодействие с воркерами через Celery и Redis
- Хранение данных в SQLite с использованием SQLAlchemy
- CLI утилита для управления воркерами из командной строки

## Структура проекта

```
tgn/
├── bot/                  # Код телеграм-бота
│   ├── __init__.py
│   ├── main.py           # Основной код телеграм-бота
│   ├── handlers.py       # Обработчики команд бота
│   └── keyboards.py      # Клавиатуры для взаимодействия с ботом
├── backend/              # Код бэкенд-сервиса
│   ├── __init__.py
│   ├── listener.py       # Логика прослушивания Redis и обновления БД
│   └── cli.py            # Код для CLI утилиты
├── database/             # Код для работы с базой данных
│   ├── __init__.py
│   ├── models.py         # Модели SQLAlchemy
│   └── operations.py     # Операции с базой данных
├── celery_app/           # Код для работы с Celery
│   ├── __init__.py
│   ├── tasks.py          # Задачи для Celery
│   └── worker.py         # Настройка воркера Celery
├── worker/               # Код для воркеров
│   ├── __init__.py
│   ├── main.py           # Основной код воркера
│   └── telethon_manager.py # Управление аккаунтами Telethon
├── config.py             # Конфигурация проекта
├── .env                  # Переменные окружения (не включены в репозиторий)
├── .env.example          # Пример файла с переменными окружения
├── requirements.txt      # Зависимости проекта
├── run.py                # Запуск бота
├── run_worker.py         # Запуск воркера
├── run_backend.py        # Запуск бэкенд-сервиса
└── run_cli.py            # Запуск CLI утилиты
```

## Установка и запуск

### Предварительные требования

- Python 3.7+
- Redis
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Установка

1. Клонировать репозиторий:
   ```
   git clone https://github.com/yourusername/tg-telethon-bot.git
   cd tg-telethon-bot
   ```

2. Создать и активировать виртуальное окружение:
   ```
   python -m venv venv
   source venv/bin/activate  # На Windows: venv\Scripts\activate
   ```

3. Установить зависимости:
   ```
   pip install -r requirements.txt
   ```

4. Создать файл `.env` на основе `.env.example` и заполнить необходимые переменные окружения:
   ```
   # Настройки Telegram бота
   TG_BOT_TOKEN=your_telegram_bot_token_here
   ADMIN_IDS=123456789,987654321

   # Настройки базы данных
   DATABASE_URL=sqlite:///tg_bot.db

   # Настройки Redis и Celery
   REDIS_URL=redis://localhost:6379/0
   ```

### Запуск бота

```
python run.py
```

### Запуск воркера

На удаленном сервере:

```
python run_worker.py
```

### Запуск бэкенд-сервиса

Бэкенд-сервис автоматически отслеживает появление новых воркеров в Redis и добавляет их в базу данных:

```
python run_backend.py
```

Можно указать интервал сканирования в секундах (по умолчанию 10 секунд):

```
python run_backend.py 30  # Сканирование каждые 30 секунд
```

### Использование CLI утилиты

CLI утилита позволяет управлять воркерами из командной строки:

```
# Вывод списка всех воркеров
python run_cli.py list

# Вывод списка только активных воркеров
python run_cli.py list --active-only

# Просмотр информации о конкретном воркере
python run_cli.py show worker_name

# Активация воркера
python run_cli.py activate worker_name

# Деактивация воркера
python run_cli.py deactivate worker_name
```

## Мониторинг воркеров

Система включает механизм мониторинга статуса воркеров в режиме реального времени:

1. **Периодические обновления статуса**: Каждые 30 секунд воркеры отправляют свой статус в Redis.
2. **Проверка статуса**: При запросе списка воркеров, бот проверяет их реальный статус через Redis.
3. **Визуализация состояния**: В интерфейсе бота воркеры отображаются со статусами "Онлайн" 🟢 или "Оффлайн" 🔴.
4. **Актуальная информация**: Статус в базе данных автоматически обновляется на основе реальной информации о доступности воркера.

Если воркер перестает отправлять обновления статуса более 60 секунд, он автоматически помечается как "Оффлайн".

## Оптимизация производительности

Для обеспечения быстрой работы бота были внедрены следующие оптимизации:

1. **Прямой доступ к Redis**: Вместо использования Celery для проверки статуса воркеров, бот напрямую обращается к Redis, что значительно ускоряет получение информации.

2. **Кэширование статусов**: Статусы воркеров и аккаунтов кэшируются в Redis с временем жизни, что позволяет быстро получать актуальную информацию без блокирующих запросов.

3. **Асинхронные операции**: Операции, требующие взаимодействия с воркерами (запуск/остановка аккаунтов), выполняются асинхронно через Celery, не блокируя работу бота.

4. **Предварительная проверка доступности**: Перед отправкой команд воркеру, бот проверяет его доступность через Redis, что позволяет избежать длительных таймаутов при обращении к недоступным воркерам.

5. **Оптимизированные запросы к базе данных**: Запросы к базе данных оптимизированы для минимизации количества обращений и объема получаемых данных.

6. **Разделение интерфейса и логики**: Бэкенд-сервис автоматически синхронизирует состояние воркеров между Redis и базой данных, что позволяет боту работать с актуальными данными без выполнения тяжелых операций.

Эти оптимизации позволяют боту быстро отвечать на запросы пользователей даже при большом количестве воркеров и аккаунтов.

## Использование

1. Запустите бота и отправьте команду `/start`
2. Используйте меню для навигации и управления воркерами и аккаунтами
3. Добавьте воркер, указав его имя и адрес сервера
4. Добавьте аккаунты Telethon к воркеру, указав номер телефона, API ID и API Hash
5. Управляйте аккаунтами: запускайте, останавливайте, проверяйте статус

## Лицензия

MIT 

## Архитектура системы

Система состоит из нескольких компонентов:

### Telegram-бот
Бот предоставляет пользовательский интерфейс для управления воркерами и аккаунтами. Через него можно просматривать статусы воркеров и аккаунтов, добавлять новые аккаунты, запускать и останавливать аккаунты. Бот взаимодействует с базой данных SQLite и с Celery для отправки задач воркерам.

### Воркеры
Воркеры - это экземпляры приложения, которые могут находиться на разных серверах и выполнять операции с аккаунтами Telethon. Каждый воркер при запуске регистрируется в Redis, отправляет периодические обновления статуса и выполняет задачи, получаемые через Celery.

### Бэкенд-сервис
Бэкенд-сервис автоматически отслеживает появление новых воркеров в Redis и добавляет их в базу данных. Он также обновляет статусы воркеров в базе данных на основе их фактического состояния в Redis. Это позволяет боту всегда иметь актуальную информацию о доступных воркерах.

### База данных
База данных SQLite хранит информацию о воркерах и аккаунтах. SQLAlchemy используется как ORM для работы с базой данных.

### Redis и Celery
Redis и Celery используются для асинхронного взаимодействия между ботом и воркерами. Redis также служит хранилищем для статусов воркеров и аккаунтов в реальном времени.

## Логика работы системы

1. **Регистрация воркера**:
   - Воркер запускается на сервере и регистрируется в Redis
   - Бэкенд-сервис обнаруживает нового воркера и добавляет его в базу данных

2. **Мониторинг статуса воркеров**:
   - Воркеры каждые 30 секунд отправляют обновления статуса в Redis
   - Бэкенд-сервис каждые 10 секунд проверяет статусы воркеров в Redis и обновляет информацию в базе данных
   - Воркер считается неактивным, если не обновлял статус более 60 секунд

3. **Управление аккаунтами**:
   - Бот отправляет задачи для управления аккаунтами через Celery
   - Воркеры выполняют задачи и возвращают результаты
   - Статусы аккаунтов обновляются в базе данных 